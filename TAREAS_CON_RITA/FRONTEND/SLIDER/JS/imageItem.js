// Definir la clase ImageItem y su constructor
class ImageItem {
    constructor(ruta, nombre, descripcion) {
        this.ruta = ruta
        this.nombre = nombre;
        this.descripcion = descripcion;
        this.index = 0; // Establecer el indice inicial en 0.
    }
}

export default ImageItem;